// Display a simple message outside a function that says,
// This is a text outside a function block

function display(x) {
  return x;
}

let message = "THis is a text outside a function block";
console.log(display(message));
